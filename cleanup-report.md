# Cleanup Report

Audit date: 2026-06-19

## Removed Generated Artifacts

- `client/dist`
- `client/coverage`
- `server/coverage`

These folders were generated by local build/test commands and are already ignored by git.

## Retained Intentionally

- `node_modules` folders were retained so the project remains runnable after this audit pass. They are ignored by git and should not be committed.
- `server/uploads` was retained as an application runtime directory. Uploaded files are deleted after processing by the resume controller.
- `.postman` and `postman` were retained as project/API testing assets.

## Repository Hygiene

- `.gitignore` covers `node_modules`, `.env`, uploads, PDF artifacts, `dist`, build outputs, and coverage outputs.
- No generated `dist` or `coverage` folders remain after cleanup.

## Recommendations

- Run `npm ci` in `client` and `server` on fresh machines instead of committing dependencies.
- Keep generated certificates/reports out of git unless they are curated documentation examples.
